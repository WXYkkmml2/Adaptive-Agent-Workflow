"""
全局配置：阈值、映射参数、权限模板。
后续阈值都集中在这里管理。
"""

# ============================================================
# D0 相关
# ============================================================

# BFS 潮流变化量阈值：当节点电压变化率低于此值时，认为影响已衰减到可忽略
BFS_VOLTAGE_CHANGE_THRESHOLD = 0.01  # 1%

# 条件数归一化的参考上限
# IEEE 14-bus 局部子矩阵条件数通常在 1~1000 量级
# 超过此值按 1.0 封顶
CONDITION_NUMBER_UPPER_BOUND = 500.0

# D0 → 基础树深 H0 的映射区间
# 格式: (D0下界, D0上界, H0值)
# D0 落在哪个区间就取对应的 H0
D0_TO_H0_MAP = [
    (0.0, 0.3, 3),   # 影响范围小 → 三层结构即可
    (0.3, 0.6, 4),   # 中等 → 四层
    (0.6, 1.0, 5),   # 影响范围大 → 五层
]

# 编排深度硬上限（不管怎么加都不能超过这个值）
H_MAX = 7

# ============================================================
# 确定性指标 C 的阈值（Step 2 用，这里先预设）
# ============================================================
C_THRESHOLD_HIGH = 0.7   # C > T1: 高确定性，保持 H0
C_THRESHOLD_LOW = 0.3    # C < T2: 低确定性，拆子树

# ============================================================
# 采样温度区间（Step 3 用，这里先预设）
# ============================================================
TEMPERATURE_MIN = 0.1
TEMPERATURE_MAX = 1.0

# ============================================================
# 电网约束阈值
# ============================================================
BUS_VOLTAGE_MIN = 0.95    # 母线电压下限 (p.u.)
BUS_VOLTAGE_MAX = 1.05    # 母线电压上限 (p.u.)
LINE_LOADING_MAX = 100.0  # 线路负载率上限 (%)

# ============================================================
# 权限三元组模板（Step 2 用到）
# ============================================================
# 每个权限三元组: (区域标识集合, 电压等级集合, 允许设备类型集合)
# 根智能体默认拥有全部权限
ROOT_PERMISSION = {
    "regions": {"all"},
    "voltage_levels": {"HV", "MV", "LV"},  # 高压、中压、低压
    "device_types": {"bus", "line", "gen", "trafo", "switch", "load"},
}